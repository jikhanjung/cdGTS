# 20260705_048 — 릴리스 0.1.6 (ICC stage 완성 · narrate · 노드그룹 계층 · L2)

> [043](20260705_043_release-0.1.5.md)(0.1.5) 이후 라운드 044~047 을 한 이미지로. 빌드/푸시는 여기서,
> 운영 배포는 사용자가 진행.

## 0.1.6 에 담긴 것 (044~047)

- **044 ICC stage 완성** — chart.ttl finer 유닛 134(Epoch/Age, 공식 색·계보) + **공표 릴리스 `ICS-2024/12`**
  (경계 175 candidate/selection → bake 175). 차트 소스토글(공표 5컬럼 / 그래프 3컬럼), 릴리스 ICC 엔드포인트.
  → seed `01_chrono`·신규 `05_icc_release`.
- **045 narrate** — bake 의 짝. `narrate_release()` 로 정의·연대·오차·이중명명을 **사실 창작 없이** 서술,
  `BoundaryRecord.narrative` 저장. `POST /api/releases/{id}/narrate/` + "ICC 서술" 탭.
- **046 Triassic age 노드그룹** — 계층을 네트웍 구조로. period 구간=그룹, 내부 age 분할점 6 + order 체인.
  **NodeGroup seedable**(자연키, 마이그레이션 없음). example-icc-partial 노드 77→90·게이트웨이 36→42·그룹 0→1.
- **047 L2 지속시간 게이트** — 게이트웨이가 덮는 모든 유닛 duration(base−top) 자동 검사. ≤0 → fail.
  authored order(L1, gap≥0 허용) 를 보완. 순수 엔진 로직.

## 빌드
- `deploy/build.sh 0.1.6` — pytest **81 passed** → `config/version.py` bump → 이미지
  `honestjung/cdgts:0.1.6`(+`latest`) 빌드/푸시.

## 운영 배포 노트 (사용자 진행)
- `git pull` → `sync_to_srv.sh` → `deploy.sh 0.1.6`(스냅샷 + 스왑).
- **마이그레이션**: 0.1.6 신규 없음. 0.1.5 를 안 거쳤다면 `chrono.0002`(Unit.color)·`graph.0003`(NodeGroup x/y)
  가 entrypoint migrate 로 자동 적용.
- **재시드 `seed --mode=replace` 필요** ⚠️: finer 유닛·공표 릴리스·Triassic 그룹(첫 seed 되는 NodeGroup)·
  example 그래프 확장. add 는 원자 skip 이라 반영 안 됨.
  - 예상: units **176** · boundaries 175 · node types 13 · graphs 4 · releases **3** · records **180**.
    example-icc-partial: 노드 **90** · 게이트웨이 **42** · 그룹 **1**(triassic-ages, 멤버 11).

## 검증
- 로컬 pytest **81 passed**, 프론트 빌드 클린. seed replace 재구축 정상(cert L1·L2 pass).
