# 20260703_025 — 배포 0.1.1 (개발/테스트 서버)

> [021](20260703_021_docker-deploy.md)~024 후속. 계산 커널 포함 첫 실배포.

## 한 일

### 빌드/푸시 (build.sh)
- `./deploy/build.sh 0.1.1` — pytest 59 → version.py 0.1.1 커밋 → `docker build` → push
  `honestjung/cdgts:0.1.1` + `latest`(543MB, numpy/scipy 포함). git push.

### 개발/테스트 서버 배포 (/srv/cdGTS)
- **도메인 미사용**(운영서버는 별도): `.env` ALLOWED_HOSTS=127.0.0.1,localhost / DEBUG=False / IMAGE_TAG=0.1.1.
- `sync_to_srv.sh` → host compose + deploy.sh. 컨테이너 `cdgts` @ `127.0.0.1:8011`(strati2026 이 8010 점유).
- 첫 배포라 빈 DB → 시드 `loaddata initial_boundaries initial_node_types` (58객체: 노드타입 12·경계 3).

### deploy.sh 방어 처리
- `backup/` 가 root 소유(사용자 sudo mkdir 산물)라 pre-deploy 스냅샷 mkdir 실패 → **배포 전체 abort** 하던 것 수정.
  이제 DB 비었거나 backup 쓰기 불가면 **경고 후 건너뜀**(배포는 계속). 소유권 해결 시 스냅샷 재개.

## 검증
- 컨테이너 `0.1.1` up, version.py=0.1.1. `/`·`/admin/login/`·`/api/node-types/` **200**, node-types 12·경계 3.
- **계산 커널 실배포 확인**: 컨테이너에 numpy 2.2.6/scipy 1.15.3, 배포 API 로 age-depth @5 → **255.0**(2σ0.354).

## 후속 (사용자)
- **스냅샷 복구**: `sudo chown -R $USER /srv/cdGTS/backup` 하면 다음 배포부터 pre-deploy 스냅샷 동작.
- **운영서버**: 사용자가 별도 진행(도메인 cdgts.paleobytes.info + nginx + certbot).
