# 20260705_071 — 릴리스 0.1.15 (선택 재렌더 폭풍 픽스)

> [069](20260705_069_release-0.1.14.md)(0.1.14) 이후 070 을 이미지화. 배포는 사용자.

## 0.1.15 에 담긴 것

- **070 대형 그래프 선택 박스 얼어붙음 픽스** — `buildView` 를 구조/선택 분리(토폴로지 시그니처 memo).
  rubber-band 선택 중 매 pointermove 마다 전체 엣지가 재렌더되던 폭풍 제거. 프론트 전용.
  (068 auto-pan 픽스는 이미 0.1.14 에 포함.)

## 빌드
- `deploy/build.sh 0.1.15` — pytest → bump → `honestjung/cdgts:0.1.15`(+`latest`).

## 운영 배포 노트 (사용자)
- `git pull` → `sync_to_srv.sh` → `deploy.sh 0.1.15`.
- **마이그레이션·재시드 불필요** — 프론트 전용.
- 배포 후 확인: 예제 4(icc-partial) 처럼 밀집한 그래프에서 좌-드래그 선택 박스가 튐/얼어붙음 없이 매끄러운지.

## 검증
- 로컬 pytest, 프론트 빌드 클린.
