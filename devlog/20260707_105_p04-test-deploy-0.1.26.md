# 20260707_105 — P04 테스트 서버 배포·검증 (0.1.26)

[P04](20260707_P04_editor-bake-vault-restructure.md)(불변 Bake + Vault 허브, devlog 102·103·104)를
테스트 서버 `127.0.0.1:8011`에 배포하고 엔드투엔드 검증. 운영은 0.1.25 유지.

## 배포

- `deploy/build.sh 0.1.26` — pytest **96 통과** 게이트 → 버전 bump 커밋 → `honestjung/cdgts:0.1.26`(+latest)
  빌드/푸시.
- `git push` → `/srv/cdGTS/deploy-dev.sh 0.1.26` — 이미지 pull + 컨테이너 스왑(DB 볼륨 유지), backend 5초 내 기동.
- **마이그레이션 자동 적용** — entrypoint `manage.py migrate`가 `releases.0004`(kind·source_graph 스키마)·
  `0005`(기존 `graph:*`→transient 백필)를 적용. 로그로 확인.

## 정정 (104의 예측)

104는 "배포 시 재시드 필요"라 적었으나 **재시드 불필요**로 확인. P04는 seed 파일을 바꾸지 않았고(신규 Release 필드는
default 있음), 스키마 변경은 entrypoint migrate로 흡수됨. 컨테이너 스왑만으로 완료.

## 엔드투엔드 검증 (테스트 서버 실데이터)

- `/api/releases/`(경량 목록) — kind·is_baseline·source_graph·record_count 정상(ICS-2024/12 = published·baseline·177).
- bake 이름 제안 = `GeologicTimeScale.Release.20260707.01`.
- `POST /api/graphs/72/bake/` → 새 불변 Release(177 boundaries, kind=bake, source=example-icc-partial).
- 목록 갱신 — bake 최신순 노출 + transient 숨김.
- Vault 표현 — release icc-chart(6 levels)·narrate(6 sections)·getRelease(177 records) 모두 렌더.

## 상태

운영 0.1.25 / 테스트 0.1.26. **운영 배포는 사용자 승인 후.** 다음 후보 = P05.1(인증 토대).
