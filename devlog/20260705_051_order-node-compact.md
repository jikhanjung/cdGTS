# 20260705_051 — order 노드 컴팩트 (세로 부등호)

> order 노드 height 가 과함(title·rule·verdict·port 라벨 4행). 작게 줄이고, 부등호를 90° 돌려
> **어느 쪽 값이 큰지** 만 시각화. 상세(gap/Δ/mode)는 툴팁·인스펙터로.

## 한 일
- `OrderNode.jsx`: 158px 4행 → **40×36px** 단일 글리프. `≥` 를 회전 —
  gap≥0(아래=older 큼) → **아래로 열림(-90°)**, gap<0(위반, 위가 큼) → 위로 열림(90°). 색: ok 초록 / 위반 빨강 / 무입력 회색.
  전체 상세는 `title` 툴팁(선후·Δ·gap·통과/위반).
- `Inspector.jsx`: order 노드 선택 시 **판정 읽기** 추가 (`아래≥위: gap … → 통과/위반`). min_gap·mode 는 기존 파라미터 폼.

## 검증
- 프론트 빌드 클린. 백엔드·시드·엔진 무관(순수 표현). 부등호 방향은 gap 부호, 색은 ok 로 분리.

## 이월
- 브라우저 육안(작은 노드에서 부등호 방향·핸들 위치) · 세로 간격(seed SP) 조정 여지.
