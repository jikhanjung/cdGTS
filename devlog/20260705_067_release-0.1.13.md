# 20260705_067 — 릴리스 0.1.13 (모바일 생성 · Science CI · 불확실성)

> [063](20260705_063_release-0.1.12.md)(0.1.12) 이후 064·065·066 을 이미지화. 배포는 사용자.

## 0.1.13 에 담긴 것

- **064 모바일 노드 생성** — 탭-투-추가(팔레트 탭 → 캔버스 탭 배치) + 롱프레스 컨텍스트 메뉴. 프론트 전용.
- **065 Science CI 검증** — `Release.is_baseline`(공표 기준) + 에디터 "공표 대비 검증"(재bake→diff→요약).
  마이그레이션 releases 0002 + 재시드(baseline 플래그).
- **066 차트 불확실성** — 각 하부경계 ±pm 를 ICC 차트에 토글 밴드·툴팁으로. 마이그레이션·시드 무관.

## 빌드
- `deploy/build.sh 0.1.13` — pytest **85 passed** → bump → `honestjung/cdgts:0.1.13`(+`latest`).

## 운영 배포 노트 (사용자)
- `git pull` → `sync_to_srv.sh` → `deploy.sh 0.1.13`.
- **마이그레이션 releases 0002 있음** ⚠️ — `migrate` 필요(Release.is_baseline 추가).
- **재시드 `seed --mode=replace` 필요** ⚠️: ICS-2024/12 에 is_baseline 플래그 설정(065 검증 기준).
  - 예상: units 177·boundary 177·releases 3·records 182. example-icc-partial 그룹 14·bake 177.
- 배포 후 확인: 폰에서 노드 탭 생성/롱프레스, 에디터 "공표 대비 검증", 차트 "± 불확실성" 토글.

## 검증
- 로컬 pytest **85 passed**, 프론트 빌드 클린. cert L1·L2 pass.
