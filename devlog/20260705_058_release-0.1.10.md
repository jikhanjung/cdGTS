# 20260705_058 — 릴리스 0.1.10 (전 period age 그룹 · 그룹 선택/병합 · 네이밍)

> [055](20260705_055_release-0.1.9.md)(0.1.9) 이후 라운드 056·057(+네이밍 docs)를 이미지화. 배포는 사용자.

## 0.1.10 에 담긴 것

- **056 전 period age 노드그룹** — Triassic 프로토타입 일반화. 11 period 를 age 세분 그룹으로
  (84 분할점 + order 체인). example-icc-partial 노드 90→**255**·게이트웨이 42→**120**·그룹 1→**11**. bake 120.
  (Carboniferous 는 subperiod 계보 문제로 이월.)
- **057 그룹 선택·병합** — 접힌 그룹을 선택 대상으로(rubber-band·Shift). 그룹+노드를 단일 계층 **병합**.
  그룹 order 위/아래 포트(crossing tie) + `onConnect` 그룹포트↔멤버 환원.
- **네이밍 docs** — README(한/영) 표제/부제 정리, `docs/naming.md`(한/영) 신설.

## 빌드
- `deploy/build.sh 0.1.10` — pytest **81 passed** → bump → `honestjung/cdgts:0.1.10`(+`latest`).

## 운영 배포 노트 (사용자)
- `git pull` → `sync_to_srv.sh` → `deploy.sh 0.1.10`. **마이그레이션 없음**.
- **재시드 `seed --mode=replace` 필요** ⚠️: `03_graphs`(전 period age 그룹). add 는 원자 skip.
  - 예상: units 176·boundaries 175·releases 3·records 180. example-icc-partial 노드 **255**·게이트웨이 **120**·그룹 **11**.
- 참고: example-icc-partial 이 255 노드로 커짐 — 에디터 로딩 체감 확인.

## 검증
- 로컬 pytest **81 passed**, 프론트 빌드 클린. cert L1·L2 pass.
