# 20260704_038 — 릴리스 0.1.4 (ICC 데이터 + 노드 UX)

> [033](20260704_033_deploy-0.1.3-reseed-prod.md)(0.1.3) 이후 라운드 034~037 을 한 이미지로. 핵심은
> **ICC 테이블 bake** + **ICS chart.ttl 시드**. 빌드/푸시는 여기서, 운영 배포는 사용자가 진행.

## 0.1.4 에 담긴 것 (034~037)

- **034 노드 폭·description** — 노드 폭 축소 + 사용자 리사이즈, 별도 `description` 필드 + 제목 툴팁.
  → **마이그레이션** `graph.0002_nodeinstance_description_width` 포함(배포 시 자동 migrate).
- **035 조립 예제** — `example-icc-partial`(GSSA·P–T·캄브리아 합본).
- **036 ICC 테이블 bake** — `bake_graph` + `POST /api/graphs/{id}/bake/` + 프론트 "ICC 테이블" 뷰.
  그래프 게이트웨이 출력을 경계 스냅샷(릴리스 `graph:<slug>`)으로 얼림.
- **037 ICS chart.ttl 시드** — 공식 chart.ttl(ICS 2024-12) 파싱. Period 이상은 `published-age`
  데이터 노드로 네트웍(example-icc-partial, 예제 3개는 파이프라인 유지), Epoch/Age 이하는 registry 만.
  chrono units 12→42 · boundaries 3→175 · node types 12→13.
- **개념 회고** — `tier-category-model`(레이어 0–6 → 티어 × 카테고리). 코드 무관·문서만.

## 빌드
- `deploy/build.sh 0.1.4` — pytest **65 passed** → `config/version.py` bump 커밋 → 이미지
  `honestjung/cdgts:0.1.4`(+`latest`) 빌드/푸시.

## 운영 배포 노트 (사용자 진행)
- `git pull` → `sync_to_srv.sh` → `deploy.sh 0.1.4`(배포 전 DB 스냅샷 + 스왑). entrypoint 가 migrate 실행
  → `graph.0002` 자동 적용.
- **재시드는 `seed --mode=replace` 필요** ⚠️: `example-icc-partial` 그래프가 이미 운영에 있어
  **add 모드는 그래프를 slug 단위로 통째 skip**(원자 컨테이너) → 이번에 추가된 33개 데이터 노드·게이트웨이가
  **반영되지 않는다**. 새 유닛/경계/노드타입까지 깨끗이 넣으려면 replace.
  (아직 정식 사용 전이라 운영 DB 갈아엎기 무방 — [032] 결정.)
  - 예상: replace 후 units 42 · boundaries 175 · node types 13 · graphs 4 · 릴리스 bake 2.
  - 그래프 ICC 테이블은 UI 에서 bake(또는 `POST /api/graphs/<id>/bake/`).

## 검증
- 로컬 pytest **65 passed**, 프론트 빌드 클린. `seed --mode=replace` 로컬 재구축 정상(inserted 385).
- 테스트 DB bake: `example-icc-partial` → 36경계 ICC(base-hadean 4567 … base-quaternary 2.58, 예제값 유지).

## 남은 것 / 이월
- `engine._certify` 층서순 정합(현재 나열순 스텁 → 큰 그래프 warn).
- finer 경계 **값**을 릴리스(candidate/selection)로 승격 → 완전한 공표 ICC 릴리스(선택).
- bayesian age-depth 커널.
