# 20260705_052 — 릴리스 0.1.7 (세로 포트 · 다중선택 · 컴팩트 order)

> [048](20260705_048_release-0.1.6.md)(0.1.6) 이후 라운드 049~051 을 한 이미지로. 빌드/푸시는 여기서,
> 운영 배포는 사용자가 진행.

## 0.1.7 에 담긴 것 (049~051)

- **049 data↔order 세로 포트** — published-age 에 `older`(위)/`younger`(아래) out 포트(타입 속성).
  order 엣지가 짧은 세로 선분·오배선 방지. `out`(우측)은 gateway 용. → seed `02_nodes`·`03_graphs`.
- **050 다중선택 조작** — 좌-드래그=선택박스, 중앙버튼=팬, Shift+클릭=추가. 선택→우클릭→그룹 발견 가능.
- **(세로 포트 period 전체 적용)** — period 체인 order 엣지 64개도 역할(older/younger)로 재배선. → seed `03_graphs`.
- **051 order 노드 컴팩트** — 158px 4행 → 40×36px, 부등호 90° 회전(큰 값 쪽 열림). 상세는 툴팁·인스펙터.

## 빌드
- `deploy/build.sh 0.1.7` — pytest **81 passed** → `config/version.py` bump → 이미지
  `honestjung/cdgts:0.1.7`(+`latest`) 빌드/푸시.

## 운영 배포 노트 (사용자 진행)
- `git pull` → `sync_to_srv.sh` → `deploy.sh 0.1.7`(스냅샷 + 스왑).
- **마이그레이션 없음**(049~051 은 코드/시드/프론트).
- **재시드 `seed --mode=replace` 필요** ⚠️: `02_nodes`(published-age 포트 추가)·`03_graphs`(order 엣지 재배선).
  add 는 원자 skip 이라 반영 안 됨. 카운트는 0.1.6 과 동일(units 176·boundaries 175·graphs 4·releases 3·records 180),
  example-icc-partial 노드 90·게이트웨이 42·그룹 1.

## 검증
- 로컬 pytest **81 passed**, 프론트 빌드 클린. 엔진 중립(cert L1·L2 pass, bake 42) 유지.
