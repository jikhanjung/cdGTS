# 20260705_055 — 릴리스 0.1.9 (폰 반응형)

> [053](20260705_053_release-0.1.8.md)(0.1.8) 이후 라운드 054(폰 대응)를 이미지화. 빌드/푸시는 여기서, 배포는 사용자.

## 0.1.9 에 담긴 것 (054)

- **054 폰 반응형·터치** —
  - 터치(`pointer: coarse`) 감지 → 드래그 팬 + 핀치 줌. 데스크톱은 좌-드래그 선택/가운데 팬 유지.
  - 에디터 3단 고정폭 → ≤820px 에선 **캔버스 풀폭 + 팔레트/인스펙터 서랍**(☰·속성 토글·backdrop·✕).
  - 미디어쿼리: topnav 가로 스크롤·toolbar wrap·여백 축소. `.view{overflow:hidden}` + `.icc/.diff{overflow-x:auto}`
    로 넓은 표는 컨테이너 내부 스크롤(바디 가로 스크롤 차단).

## 빌드
- `deploy/build.sh 0.1.9` — pytest **81 passed** → bump → `honestjung/cdgts:0.1.9`(+`latest`).

## 배포 노트 (사용자)
- `git pull` → `sync_to_srv.sh` → `deploy.sh 0.1.9`. **마이그레이션·재시드 없음**(프론트 전용).

## 이월
- 폰 실제 육안 검증 · 터치 노드 생성(탭-투-추가) · 롱프레스 컨텍스트 메뉴.
