# 20260703_022 — 이미지 push (honestjung/cdgts 0.1.0)

> [021](20260703_021_docker-deploy.md) 후속. 배포 셋업에서 "사용자 몫"으로 남겼던 Docker Hub push 완료.

## 한 일

- `docker push honestjung/cdgts:0.1.0` · `:latest` — **둘 다 성공**.
  동일 digest `sha256:698e54fb…` (latest == 0.1.0 확인). 로그인 계정 `honestjung`.
- 일부 베이스 레이어는 기존 `honestjung/strati2026` 에서 mount 되어 빠르게 업로드.

## 상태

- 이제 각 호스트에서 `deploy.sh 0.1.0` 이 `docker pull` 로 받아 배포 가능.
- 021 의 "못 한 것" 중 **push 는 해소**. 남은 것: 실제 `/srv/cdGTS` 셋업 + 배포 실행(각 서버 sudo/호스트 고유),
  nginx 리버스 프록시·maintenance(후속).

## 다음

- 각 서버 배포 실행(개발/테스트 + 프로덕션) → 확인 후 **계산 커널(engine)** 착수.
