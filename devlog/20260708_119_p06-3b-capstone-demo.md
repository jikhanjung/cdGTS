# 20260708_119 — P06.3b 캡스톤 데모 (공분산 게이트 + clamp reconcile 눈으로)

[P06](20260708_P06_arc-a-science-engine.md) 06.1–06.3 을 **볼 수 있게** 하는 멱등 관리 명령 `seed_demo`.
시드엔 authored clamp/공유성분 데이터가 없어 기능이 안 보였음 → 메인 시드 안 건드리고 데모 데이터만 add.

## `manage.py seed_demo` (releases/management/commands/seed_demo.py)

**1. 공분산 게이트 대비 그래프 2개** — 값·± 동일, **공유 계통 태그만 다름**:
- `demo-cov-independent` — 인접 두 Age 경계(249·247, 각 1σ 1.5), 독립 → **L1b WARN**(2σ_gap≈4.24 > gap 2).
- `demo-cov-shared` — 같은 값/±, 둘 다 `decay-238U`(σ 1.4) 공유 → Cov 1.96 → σ_gap 0.76, 2σ 1.52 < 2 → **L1b PASS**.
- Editor 에서 각각 열어 Evaluate → Results 패널 consistency 칩(L1b amber vs green)으로 공분산 효과 확인.

**2. 공표 ICS-2024/12 에 authored clamp 2개**(Vault → Clamps 탭):
- `range [250,253]` on base-triassic(251.9) → **honored**.
- `pin 536.0` on base-cambrian(538.8) → **violation**(L3a) → staff **Reconcile(L3b)** 누르면 값이 536.0 으로 이동.

멱등(get_or_create + 재빌드). 실행: `python manage.py seed_demo`.

## 검증

test_seed.py +1 (`test_seed_demo_capstone`): 독립=warn·공유=pass, cambrian pin violation → reconcile → 536.0 → honored.
전체 **pytest 137 passed**.

## 배포 노트

이미지에 명령 포함. 테스트 서버 스왑 후 `docker exec cdgts python manage.py seed_demo` 1회 실행해야 데모 데이터 생성
(entrypoint 는 migrate 만, seed 는 안 함). 야간 prod 미러 sync 시 사라지므로 필요할 때 재실행.
