# 20260709_126 — 릴리스 0.1.35 (이미지 빌드·푸시)

이번 세션 작업(devlog 123·124·125)을 묶어 이미지 `honestjung/cdgts:0.1.35`(+ latest) 빌드·푸시.
**운영 배포는 사용자가 실행**(host 동기화 + deploy.sh). 본 로그는 릴리스 기록.

## 담긴 것 (0.1.34 → 0.1.35)

- **P06.4a** — 비동기 평가 잡 + 워커 인프라(EvalJob·run_worker·하이브리드 라우팅), 커널 해석적 유지. (123)
- **L2/L3 게이트 후속** — L2 fail 상세 note(퇴화 경계쌍 라벨). 도메인 임계 warn 은 보류. (124)
- **retype diff 실데모** — `diff_releases` shape 축(exact→분포) + `seed_demo` Cryogenian GSSA/GSSP
  릴리스 쌍 + 프론트 Shape diff 섹션. (125)

## 빌드

- `deploy/build.sh 0.1.35` — pytest **147 passed** → `config/version.py` bump/commit(9830a28) →
  `docker build` → push `0.1.35` + `latest`. digest `sha256:cae62bb9…`.
- 앞선 커밋 2ed2249 에 기능/문서/devlog 일괄 커밋(이미지 워킹트리 = 커밋 상태와 일치).

## 배포 시 주의 (사용자 실행)

- **worker 서비스 신규** — `deploy/host/docker-compose.yml` 에 `cdgts-worker`(같은 이미지,
  `manage.py run_worker`) 추가됨. `sync_to_srv.sh` 로 host/* 동기화 후 deploy 하면 web+worker
  두 컨테이너가 뜬다. entrypoint 는 인자 있으면 그대로 exec(worker), 없으면 web(migrate+gunicorn).
- **마이그레이션** — `engine.0003_evaljob` 자동 적용(entrypoint migrate). seed 무변경 → 재시드 불필요.
- **캡스톤/retype 데모(선택)** — 운영에서 보려면 `docker exec <컨테이너> python manage.py seed_demo` 1회.

## 다음

- 배포(사용자) → 브라우저 검증(비동기 평가 UX·Vault Shape diff).
- 그다음 **레퍼런스 노드**(Reference 테이블, DOI 중심 — 데이터/모델 source 링크) 작업 착수.
