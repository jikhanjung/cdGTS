# 20260705_053 — 릴리스 0.1.8 (세로 포트 확장 · 평가순서 문서)

> 0.1.7 빌드 후 사용자가 서버에서 만든 커밋(369d8eb·53a1a41) 을 이미지화. 빌드/푸시는 여기서, 배포는 사용자.

## 0.1.8 에 담긴 것

- **369d8eb 세로 order 포트 일관화** —
  - `GroupNode`: 경계 넘는 older/younger 핸들을 우측 출력에서 **위(older)/아래(younger)** 로 이동, 여러 멤버 기여 시 가로 분산.
    입력=target·출력=source. Editor 가 그룹 핸들에 port 이름 태깅.
  - seed `02_nodes`: `age-depth-model` 에 older/younger **out 포트 추가**(published-age 미러). 엔진은 노드당 한 분포를
    전 출력 포트에 흘리므로 순수 배선(커널 불변).
  - CSS: `.vport` 크기/테두리 override 제거 → 기본 in/out 핸들과 동일 크기, 색만 order 보라.
- **53a1a41 문서** — 평가는 **의존성 위상정렬**이지 연대순이 아님. order 노드는 값 안 바꾸는 **말단 sink**,
  판정은 `_certify`(L1) 에서 post-hoc 소비. order-check vs 상대연대 전파 vs 피드백 제약 구분. (KR/EN 쌍)
- **테스트 픽스** — age-depth 출력 포트 3개(horizon_age·older·younger) 반영(`nodes/tests.py`).

## 빌드
- `deploy/build.sh 0.1.8` — pytest **81 passed** → bump → `honestjung/cdgts:0.1.8`(+`latest`).

## 배포 노트 (사용자)
- `git pull` → `sync_to_srv.sh` → `deploy.sh 0.1.8`. 마이그레이션 없음.
- **재시드 `--mode=replace` 필요** ⚠️: age-depth 새 포트(`02_nodes`).
