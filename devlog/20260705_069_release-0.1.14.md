# 20260705_069 — 릴리스 0.1.14 (선택 박스 x 튐 픽스)

> [067](20260705_067_release-0.1.13.md)(0.1.13) 이후 068 을 이미지화. 배포는 사용자.

## 0.1.14 에 담긴 것

- **068 선택 박스 x 튐 픽스** — `autoPanOnSelection={false}`. 좌-드래그 선택 시 가장자리 auto-pan 이
  viewport 를 옮겨 박스 x 가 튀던 문제 제거. 프론트 전용.

## 빌드
- `deploy/build.sh 0.1.14` — pytest **85 passed** → bump → `honestjung/cdgts:0.1.14`(+`latest`).

## 운영 배포 노트 (사용자)
- `git pull` → `sync_to_srv.sh` → `deploy.sh 0.1.14`.
- **마이그레이션·재시드 불필요** — 프론트 전용(0.1.13 에서 이미 releases 0002 + 재시드 반영).
- 배포 후 확인: 에디터에서 좌-드래그 선택 박스가 가로로 매끄럽게, 튐 없이 그려지는지.

## 검증
- 로컬 pytest **85 passed**, 프론트 빌드 클린.
