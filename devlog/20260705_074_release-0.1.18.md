# 20260705_074 — 릴리스 0.1.18 (예제 4 order 노드 정돈)

> 예제 ④(example-icc-partial, 270 노드)의 order 노드를 연결된 위/아래 노드 사이에 정렬. 배포는 사용자.

## 한 일 (`seed/03_graphs.json`, 데이터만)
- order 노드 137개를 각자의 `younger`·`older` 두 소스 경계 노드의 **세로 중점**으로 이동(x 열은 유지).
  - 최상위 x=1950 열(35): 상위 경계 사이 정확한 중점.
  - 그룹 내부 age-order(각 그룹 5~10개): age 노드 사이 중점(70px 간격, 겹침 0).
  - 최상위 x=2600(23, period↔age 교차): 중점 배치 + 맥락별 x-열 최소간격(46px) 스윕으로 겹침 9곳 해소.
- 결과: 102개 이동, 잔여 겹침 0. 노드 270·order 137 개수 불변. diff 는 `"y":` 라인만.

## 빌드
- `deploy/build.sh 0.1.18` — pytest → bump → `honestjung/cdgts:0.1.18`(+`latest`).

## 운영 배포 노트 (사용자)
- `git pull` → `sync_to_srv.sh` → `deploy.sh 0.1.18`.
- **⚠️ 재시드 필요**: 레이아웃은 시드 데이터라 `seed --mode=replace` 로 재적용해야 반영됨(코드 아님).
  - dev 미리보기: 로컬 DB 에 `python manage.py seed --mode=replace` 로 확인 가능.
- 확인: 예제 4 에서 order(≥) 노드가 위/아래 경계 노드의 세로 중간에 놓이는지.

## 검증
- 시드 재적용 OK(scratchpad), 겹침 0. 프론트 빌드 클린.
