# 20260705_043 — 릴리스 0.1.5 (노드그룹 · order 제약 · ICC 차트)

> [038](20260704_038_release-0.1.4.md)(0.1.4) 이후 라운드 039~042 를 한 이미지로. 빌드/푸시는 여기서,
> 운영 배포는 사용자가 진행.

## 0.1.5 에 담긴 것 (039~042)

- **039 노드그룹** — 휴면 `NodeGroup` 을 컨테이너+접기+드릴인으로. 접으면 경계 넘는 엣지가 그룹 입출력 핸들로
  자동 정합, 드릴인 스텁은 `멤버·포트 ↔ 외부` 표기, 우클릭 컨텍스트 메뉴. → **마이그레이션 `graph.0003`**(NodeGroup x/y).
- **040 order 제약** — 휴면 `order` clamp 를 두 경계 **선후 검사**로(세로 핸들 아래=older, mode hard/warn, Δ).
  `_certify` L1 을 authored 제약으로 판정. → **seed `02_nodes` 변경**(order 포트/params 재설계).
- **041 예제4 세로 정렬** — `example-icc-partial` 36경계를 연대순 세로 컬럼 + order 노드 35 체인 → L1 pass.
- **042 ICC 차트** — `GET /api/graphs/{id}/icc-chart/` + "ICC 차트" 뷰(Eon/Era/Period 중첩 컬럼, 로그/선형).
  **공식 ICS 색**(chart.ttl `schema:color`) → `chrono.Unit.color`. → **마이그레이션 `chrono.0002`**.

## 빌드
- `deploy/build.sh 0.1.5` — pytest **77 passed** → `config/version.py` bump → 이미지
  `honestjung/cdgts:0.1.5`(+`latest`) 빌드/푸시.

## 운영 배포 노트 (사용자 진행)
- `git pull` → `sync_to_srv.sh` → `deploy.sh 0.1.5`(배포 전 DB 스냅샷 + 스왑). entrypoint migrate 가
  **`graph.0003`·`chrono.0002`** 자동 적용.
- **재시드 `seed --mode=replace` 필요** ⚠️: order 노드타입 변경 + 예제4 그래프 확장 + 유닛 색 주입.
  add 는 nodetype/그래프를 자연키·slug 로 skip 하므로 반영 안 됨.
  - 예상: units 42(색 포함) · boundaries 175 · node types 13 · graphs 4 · example-icc-partial 노드 77(+order 35).

## 검증
- 로컬 pytest **77 passed**, 프론트 빌드 클린. seed replace 로컬 재구축 정상.

## 남은 것 / 이월
- 브라우저 육안 검증(그룹 드릴인·우클릭·order 세로핸들·ICC 차트).
- order 강제(reconcile, joint) · L2 지속시간 · finer 경계 값 릴리스화 · narrate.
