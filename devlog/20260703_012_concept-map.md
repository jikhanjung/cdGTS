# 20260703_012 — 통합 개념 지도

> 브레인스토밍 단계 devlog. 요약 수준.

## 한 일

`docs/concept-map.md` (+ `_en`). 지금까지 12개 문서를 한 장으로 잇는 상위 지도. 새 주장 없이 봉합.
- **한 줄 논지 + 레이어 척추 0–6**(표 + mermaid).
- **문서 지도:** 개념(2) / 사례(3) / 스키마·설계(7).
- **수렴점 5개(알맹이):**
  1. provenance 깊이 = 하나의 축 (정합성 레벨·분포 충실도·순환 해소가 모두 종속).
  2. clamp가 통일자 (GSSA=pin=δ, 순환 절단, 분포 연산).
  3. ICC/GTS=bake/narrate 이분법 반복 (게이트·경쟁모델·diff·분포).
  4. 게이트웨이/네트워크 2계층 반복.
  5. Layer 5 = 여러 이름의 한 노드 (전역종합=게이트=joint=공분산).
- **미션 재정의** + 상태(정리됨/열림) 요약.
- README(한/영) 최상단에 "여기서 시작" 진입점으로 추가.

## 커밋

- (이 커밋) concept-map KR/EN + README 진입점 + devlog 012.

## 다음 후보

- 스키마 v0 → v1 승격(열린 질문 반영본).
- 또는 남은 열린 질문 중 하나(최소 clamp 집합, 식별자 lineage 형식 등).
- 실제 데이터 포맷/스택은 여전히 전면 미정 — 착수 시점 논의.
